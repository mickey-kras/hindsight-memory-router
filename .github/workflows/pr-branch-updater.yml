name: pr branch updater

on:
  workflow_call:
    secrets:
      PR_BRANCH_UPDATE_TOKEN:
        description: Repository token with Contents, Pull requests, and Workflows write access
        required: false
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    name: update
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: main
          persist-credentials: false
      - name: Update open PR branches
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          github-token: ${{ secrets.PR_BRANCH_UPDATE_TOKEN || github.token }}
          script: |
            const { run } = require('./.github/scripts/pr-branch-updater.cjs');
            await run({ github, context, core });
