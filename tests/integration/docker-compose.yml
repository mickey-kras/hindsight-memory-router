services:
  hindsight:
    image: node:26-alpine
    working_dir: /workspace
    command: ["node", "stub-hindsight.js"]
    environment:
      STUB_HINDSIGHT_LOG: /state/hindsight.jsonl
    expose:
      - "8888"
    volumes:
      - ./stub-hindsight.js:/workspace/stub-hindsight.js:ro
      - ./tmp/stub/state:/state
    networks:
      - internal

  memory-router:
    image: hindsight-memory-router:ci
    depends_on:
      - hindsight
    environment:
      MEMORY_ROUTER_PORT: "8890"
      MEMORY_ROUTER_TOKEN: test-router-token
      HINDSIGHT_BASE_URL: http://hindsight:8888
      HINDSIGHT_API_KEY: test-hindsight-key
      QUARANTINE_PUBLIC_KEY: ${QUARANTINE_PUBLIC_KEY:?missing QUARANTINE_PUBLIC_KEY}
      QUARANTINE_OBJECT_DIR: /state/quarantine/objects
    ports:
      - "127.0.0.1:8890:8890"
    volumes:
      - ./tmp/stub/review:/volume1/reports/hindsight-review
      - ./tmp/stub/quarantine:/state/quarantine
    networks:
      - internal

networks:
  internal:
    driver: bridge
